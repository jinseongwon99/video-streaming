// 비밀번호 확인 추가 기능 (필요시 구현)
document.getElementById('withdrawForm').addEventListener('submit', function (event) {
    const password = document.getElementById('password').value;

    // 여기에 비밀번호 확인 로직 추가 가능 (예: 비밀번호 길이 체크 등)
});